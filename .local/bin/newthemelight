#!/bin/sh
# Updates the wallpaper and theme with pywal

# st looks horrible without restarting instances, so ...
echo This will close all instances of st. Are you sure?
read oksure

# set the wallpaper
wal -li ~/Desktop/personal/anime/light_wallpapers/
killall st
